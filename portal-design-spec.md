# 设计 Spec · featon0526 交互作品集导航主页（优化版三方向）

## 1. 项目是什么
为 `featon0526.github.io` 个人站点做一个**作品集导航主页（portal）**。用户已有 13 个独立 HTML 作品页（3D 交互 / 数据可视化 / 声音音乐），缺一个统一入口把它们串起来。当前已有一版功能正常的 `index.html`（浅色、左侧分组导航 + 右侧 iframe）。本次目标是**在保留功能骨架的前提下，产出 3 个视觉方向截然不同的优化版**供用户挑选，选定后再落地为正式 `index.html`。

## 2. 目标受众与使用场景
- 受众：课程老师 / 同学 / 招聘方 / 普通访客（中国高中生独立开发者的作品集）。
- 场景：桌面浏览器投屏讲解 + 手机端浏览；首屏要在 3 秒内让人看懂「这是个作品集、左边选、右边看」。
- 设备：响应式。桌面 ≥1024px 双列；平板/手机 ≤820px 自动堆叠为「上导航 + 下内容」。

## 3. 核心信息与内容要点（导航数据，三版必须完全一致）
左侧导航分 3 组，共 13 个作品，点击后在右侧 iframe 加载对应页面（相对路径，与主页同级）：
- **3D 与空间交互（4）**
  - `3d.html` — 3D 粒子手势高阶交互系统
  - `3d_1.html` — WebCamera 3D 粒子交互系统
  - `3d_fig.html` — AI 手势识别与粒子控制
  - `3d_menu.html` — 未来全息点餐系统
- **数据可视化（7）**
  - `red.html` — 红楼梦人物关系图
  - `MUC.html` — 漫威电影宇宙角色网络
  - `TMDB-movie.html` — 星球大战角色数据获取工具
  - `wordcloud.html` — 《流浪地球》词云图
  - `wuhan.html` — 武汉大学品牌声誉深度分析报告
  - `echarts.html` — ECharts 中国地图可视化
  - `news.html` — 聚合新闻 · 实时热点资讯
- **声音与音乐（2）**
  - `birdsound.html` — 鸟语解码器
  - `city_music_pulse.html` — 城市音乐脉搏

## 4. 情感基调与气质关键词
专业但不高冷、有作者感、能让人记住「这是个人作品集而非模板站」。三版分别走：A 大胆俏皮 / B 冷静克制（开发者作品集气质）/ C 安静留白（东方空寂）。

## 5. 输出格式与尺寸（必填，三版统一）
- 形态：**网页**（非 PPT、非信息图）。固定视口流体布局，不写死像素画布。
- 结构硬约束（三版都必须满足）：`header`（站标题+作品数）→ `main`（flex 左右两列）→ `footer`（版权+技术说明）。
- `main` 左列 = 导航（分组、可点击）；右列 = 内容区，内含一个 `<iframe>`，**点击左侧链接时把对应页面 src 设进 iframe**，右侧顶部有「当前作品名 + ↗新窗口打开」。
- 首次进入显示欢迎浮层；点击链接后隐藏浮层并高亮当前项；地址栏 hash 深链（刷新可恢复）。
- 移动端（≤820px）：左右两列堆叠为上下，导航变顶部（可横向滚动或竖向滚动），iframe 占下方。
- 纯 HTML/CSS/JS 单文件；**除 Google Fonts 外禁止其他外部 CDN**（保证离线/部署后都稳）；**不依赖任何图片素材**（用排版/几何/SVG 表达，避免 slop 装饰图）。
- 可读性底线：正文 ≥14px、注释 ≥12px、正文对比度 ≥4.5:1。
- 反 AI slop：禁用 emoji 当图标（用 inline SVG 或纯文字）、禁用激进紫渐变、禁用「圆角卡+左彩 border accent」套路、禁用 GitHub-dark 均匀深蓝+霓虹 glow 这一种偷懒解。

## 6. 已知约束
- 链接必须用相对文件名（主页与 13 个作品页同级）。
- 中文 UI，引号用「」。

## 7. 视觉母题假设（三版差异化，避免换皮）
- **A · Pure-CSS Art（Lynn Fisher）**：用纯 CSS 几何形拼出有「作者签名感」的视觉——如 header 处的几何 logo 在不同断点重组形状（响应式变形彩蛋）、扁平高对比色块分区、错位叠放。母题＝「几何即插画」。结构上加一点 playful：导航项 hover 时色块翻转/位移。
- **B · Brittany Chiang 暗色编辑作品集**：深海军底 + 单薄荷青 accent + 等宽字编号标签（01/02…）+ 左侧固定栏锚点高亮 + hover 下划线滑入。母题＝「开发者作品集的事实标准」。结构上左栏为 fixed 侧栏、右内容 iframe 占满。
- **C · 原研哉「白」**：暖白底 + 发丝灰分隔 + 衬线 display + 单点朱红 + 极致但有构图的留白。母题＝「白是容纳想象的容器」。结构上左栏为带序号的极简索引、右 iframe 用发丝边框裱框 + 下方 caption。

三版布局骨架必须互异（至少一项结构性不同），不许只换色换字体。
